import rollupCoreConfig from './rollup.config';
import { terser } from 'rollup-plugin-terser';

export default {
    ...rollupCoreConfig,
    output: [
        {
            format: 'umd',
            file: 'dist/index.umd.min.js',
            name: 'Edits',
        },
    ],
    plugins: [
        ...rollupCoreConfig.plugins,
        terser(),
    ],
};